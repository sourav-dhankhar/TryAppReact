import React from "react";

const ToolbarButtonCover = (props) => {
    return (
        <React.Fragment>
            <li>{props.children}</li>
        </React.Fragment>
    )
}

export default ToolbarButtonCover;